# ============================================
# Configuration du serveur
# ============================================
PORT=3007

# ============================================
# Base de données MongoDB
# ============================================
MONGO_URI=mongodb://127.0.0.1:27017/DarnaDB

# ============================================
# Configuration JWT (Authentification)
# ============================================
#  IMPORTANT: Changez cette clé secrète en production!
# Générez une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=yourSecretKeyChangeThisInProduction

# ============================================
# Configuration Email (Nodemailer)
# ============================================
# Pour Gmail, vous devez utiliser un "App Password" au lieu du mot de passe normal
# 1. Activez la vérification en 2 étapes sur votre compte Google
# 2. Allez dans: https://myaccount.google.com/apppasswords
# 3. Générez un mot de passe d'application pour "Mail"
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password

# ============================================
# Configuration Firebase
# ============================================
# Le fichier firebase-service-account.json doit être placé à la racine du projet
# Téléchargez-le depuis: https://console.firebase.google.com
# Projet: yosra-ffae2 > Paramètres > Comptes de service > Générer une nouvelle clé privée
# 
# Les notifications Firebase seront automatiquement activées si le fichier existe
# Les notifications seront enregistrées dans Firestore (collection 'notifications')
# Les push notifications seront envoyées via FCM (Firebase Cloud Messaging)

# ============================================
# Notes importantes
# ============================================
# 1. Ne commitez JAMAIS ce fichier .env dans Git (déjà dans .gitignore)
# 2. Le fichier firebase-service-account.json doit aussi être présent à la racine
# 3. Pour les notifications Firebase, assurez-vous que:
#    - Le fichier firebase-service-account.json existe
#    - Les collections Firestore 'notifications' et 'userTokens' seront créées automatiquement
#    - Le scheduler traite les rappels toutes les 5 minutes
